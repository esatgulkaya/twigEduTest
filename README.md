# Group Array Elements Util

After clonning repository, in project's the root directory

Run `npm install` to install node app

Run `npm test` to test

Run `npm run test:coverage` to create code coverage report
